# NiFiMCP Server Environment Variables

# Base URL of the NiFi instance REST API
# Example: NIFI_BASE_URL="http://localhost:8080/nifi-api"
NIFI_BASE_URL="https://localhost:8443/nifi-api"

# Optional: Default NiFi credentials if not provided by the MCP client
# These are used as a fallback or for server-to-server scenarios if applicable.
# For user-driven interactions, credentials should ideally come from the MCP client.
# NIFI_DEFAULT_USERNAME="your_nifi_user"
# NIFI_DEFAULT_PASSWORD="your_nifi_password"

# Logging level for the server
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL="INFO"